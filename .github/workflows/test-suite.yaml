name: Test Suite

on:
  workflow_dispatch: {}
  push: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  unit-test-go:
    uses: aileron-projects/_scripts/.github/workflows/unit-test-go.yaml@main
  unit-test-qemu:
    uses: aileron-projects/_scripts/.github/workflows/unit-test-qemu.yaml@main
